# refactor-audit

Audit a Python source tree and rank refactoring candidates using churn ×
complexity, with optional lizard, ruff, and vulture annotations. The audit is
read-only and emits Markdown plus a structural baseline JSON file.

## Install

```
/plugin marketplace add pvliesdonk/claude-plugins
/plugin install refactor-audit
```
